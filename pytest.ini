[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    unit: marks tests as unit tests
    negative: marks tests as negative/failure tests
    regression: marks tests as full module regression tests
    integration: marks tests as integration tests hitting real services
